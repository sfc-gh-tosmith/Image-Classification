spec:
    containers:
        - name: image-service
          image: /tutorial_db/data_schema/tutorial_repository/image_service
          env:
            SERVER_PORT: 5000
          readinessProbe:
            port: 5000
            path: /healthcheck
    endpoints:
      - name: imageendpoint
        port: 5000
        public: true